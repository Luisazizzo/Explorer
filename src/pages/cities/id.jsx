import styles from "./id.module.scss";

export default function City() {
  return (
    <div className={styles.Activity}>
      {/* <h1>{city.name}</h1>
      <p>{city.content}</p>
      <p>{city.meta_title}</p> */}
    </div>
  );
}
